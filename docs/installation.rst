============
Installation
============

At the command line::

    $ easy_install django-bazaar

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-bazaar
    $ pip install django-bazaar